import string
def CreateTable():
    alphabet,table = string.ascii_uppercase * 2, []
    print(alphabet)
    for i in range(len(alphabet)):
        for j in range(i, i + len(alphabet)):
            # table[i][j] =
            pass


CreateTable()